import logoImage from '../assets/logo.png';

const Logo = () => (
    <div class="mb-8">
        <img
            src={logoImage}
            alt="Namy.ai Logo"
            style={{ height: 'auto' }}
        />
    </div>
);

export default Logo;
